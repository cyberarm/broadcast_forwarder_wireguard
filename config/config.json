{
    "service_ports": [ // # Array of ports to monitor for broadcasts
        24298
    ],
    "repeat_to_interfaces": [
        {
            "interface":"192.168.195.0", // # IPV4 address of vpn interface with a 0 for the last bitmask
            "start":2,                   // # IP range start
            "end":5                      // # IP range end
        }
    ]
}